import 'package:flutter/material.dart';

class MainDesktopView extends StatelessWidget {
  const MainDesktopView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
